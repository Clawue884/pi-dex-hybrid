# Basic deterministic math helpers

def sqrt(y):
    if y > 3:
        z = y
        x = y // 2 + 1
        while x < z:
            z = x
            x = (y // x + x) // 2
        return z
    elif y != 0:
        return 1
    return 0

def min(a, b):
    return a if a < b else b
